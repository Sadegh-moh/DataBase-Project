{
  "size": 20,
  "track_total_hits": false,
  "timeout": "45s",
  "query": {
    "bool": {
      "must": [ { "match": { "text": "blues" } } ],
      "must_not": [ { "match": { "text": "sad" } } ]
    }
  }
}
